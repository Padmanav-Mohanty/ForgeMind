[TEMPLATE]
# Hugging Face token (used for huggingface.co downloads; never sent to GitHub)
HF_TOKEN=YOUR_HUGGINGFACE_TOKEN

# Optional GitHub token (raises GitHub API rate limits for GHPR repo-id
# resolution; read from the environment only, never from .env)
# GITHUB_TOKEN=your_github_pat
